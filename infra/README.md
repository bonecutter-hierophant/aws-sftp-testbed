# Infrastructure

This directory owns deployable infrastructure definitions.

The MVP infrastructure should live under `infra/cloudformation/` and should be driven by AWS CLI wrapper scripts in `scripts/`.
