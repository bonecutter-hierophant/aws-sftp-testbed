# Architecture

This directory owns repo-level structure and architecture diagrams.

- `project-structure.md` describes the durable directory layout.
- `testbed-lifecycle.puml` sketches the intended deploy, test, stop, and destroy flow.

Keep this directory updated when ownership boundaries, script responsibilities, or infrastructure shape changes.
